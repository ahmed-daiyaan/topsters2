import 'package:flutter/material.dart';

class CrazyPicker extends StatelessWidget {
  const CrazyPicker();
  @override
  Widget build(BuildContext context) {
    // ignore: prefer_const_constructors
    return const Center(child: Text("Experimental Feature Only"));
  }
}
